void main() {
    int counter;
    int limit = 3;
    counter = 10;

    // Loop while counter is > limit (3)
    // counter will decrement from 10 to 3.
    while (counter > limit) { 
        counter = counter - 1;
    }

    // At the end: counter = 3, limit = 3
}
